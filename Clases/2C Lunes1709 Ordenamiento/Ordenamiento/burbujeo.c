#include "ordenamiento.h"

int ordenarBurbujeo(int* vec)
{
    int *p = vec;

    for(int i=0; i<SIZE; i++)
    {
        for(int j=0; j< SIZE - i - 1; j++)
        {
            if(vec[j]>vec[j+1])
            {
                int* temp = j;

            }

        }
    }
}
